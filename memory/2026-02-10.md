# 2026-02-10

## Resumen guardado antes de reiniciar sesión

### Resumen ejecutivo
- OpenClaw funcionando en Mac: WhatsApp enlazado, gateway activo, DM allowlist para +525521293811.
- Arquitectura growthsuite identificada como multi-front + multi-servicios backend:
  - Fronts React/Vite (admin, cash, centro, comandero, monitor, etc.).
  - Backends AdonisJS/TypeScript + PostgreSQL (auth, order, cash, centro, inventory, bot).
  - Flujo WhatsApp: `impulsobotwhats` (webhook) -> `pos_bot_api` (engine bot) -> `pos_order_api` (acciones/reportes).
- Aviso de billing/quota interpretado como probable límite temporal/rate en canal OAuth, no caída total de WhatsApp ni del proyecto.

### Pendientes
1. Estabilizar OAuth por carga (bajar concurrencia).
2. Convertir análisis técnico en backlog priorizado.
3. Endurecer seguridad webhook WhatsApp (firma/replay/rate limit).
4. Fortalecer identidad/permisos por rol (no solo teléfono).
5. Crear runbook de incidentes + healthcheck end-to-end.

### Decisiones
- Continuar con OAuth (sin migrar a API key directa por ahora).
- Usar `/new` para reset cuando el contexto esté alto.
- Antes de resetear, guardar resumen y pendientes.
- Preferencia operativa nueva: siempre pedir permiso antes de tocar código; dar feedback/plan primero; trabajar con estándares “pro” y sin salir del alcance acordado.
